## Clothing Utility 1.8.0

- Export all custom clothing assigned to a selected blacklist group as an
  editable folder or one ZIP, with whole-drawable and per-texture restrictions
  handled correctly.
- Reimport edited exports under their original names and paths, with automatic
  backups of every replaced YDD/YTD.
- Fix automatic blacklist reads and writes targeting the Lua example comment
  instead of the live freemode blacklist table.

## Asset Studio 1.3.0

- Add fixed-step preview rotation controls.
- Add a `PARTS` menu for hiding individual model meshes from previews and saved
  inventory WebPs.
