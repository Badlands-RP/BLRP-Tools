## Clothing Utility 1.7.3

- Fix polygon analysis for malformed third-party YDDs that contain renderable index buffers but declare zero triangle/index metadata.
- These models now show their real polygon count in QC and the optimiser instead of `HIGH 0`.
- LOD generation no longer fails its Medium reduction check solely because the source metadata declared zero polygons.
