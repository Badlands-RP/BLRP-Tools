## Clothing Utility 1.7.4

- Fix model imports failing YMT validation on repositories with legacy prop texture metadata.
- Preserve existing component texture metadata while appending new clothing and props.

## Updater

- Show long update notes in a resizable, scrollable window.
- Keep the Install and Cancel buttons visible regardless of release-note length.
