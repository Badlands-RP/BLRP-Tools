## Property Mapper

- Import panel property XML, review every placed object, and export deployable YMAP files.
- Optionally export the matching manifest.
- Open the generated map directly in BadWalker's GTA 3D renderer to check prop placement, rotation, and clipping.
