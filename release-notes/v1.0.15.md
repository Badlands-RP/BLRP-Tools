## Updater reliability

- Detect other BLRP tool windows that are still hosting the installed Hub executable.
- Prompt you to close remaining tools and retry instead of failing while replacing `BLRP.Tools.exe`.
- Keep the update pending safely without forcibly closing tools or risking unsaved work.

## Clothing Utility 1.7.1

- Fix LOD optimisation when the selected EUP source is an individual `clothing_addon_*` folder.
- Resolve the collection YMT from that addon directly instead of incorrectly appending the addon folder a second time.
